<%= form_for(@acroynm) do |f| %>
  <% if @acroynm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acroynm.errors.count, "error") %> prohibited this acroynm from being saved:</h2>

      <ul>
      <% @acroynm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
